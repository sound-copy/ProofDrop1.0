# Config Ledger (what changed + why)
> Add a short line per PR that modifies config/build/CI

- 2025-11-07 — PR #NN — files: electron-builder.yml — reason: include ffmpeg runtime — impact: packaging macOS — rollback: revert file, rebuild
